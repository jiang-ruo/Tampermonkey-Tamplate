
# Tampermonkey-Tamplate - 添加编译时间

版本: v1.1

日期: 2025.10.26

## 引言

### 背景与目的

方便观察main.js是否更新

## 模块设计
### 1. 优化package.json的devDependencies

#### 1.1.1 - vite

### 2. 添加编译时间

#### 1.1.2 - vite

1. headerPlugin中添加一个开关{ addExportTime: boolean }，默认关闭
2. addExportTime为true时，在导出的脚本中添加编译时间

### 2. vue
#### 2.4. vue打包后的css在独立的文件中

1. 将headerPlugin分为两部分header-load-plugin和header-post-plugin
2. 新增plugins/vue-css.ts文件，实现vue-css-plugin插件
3. header-load-plugin负责加载header到bundle中，名称为header.txt
4. vue-css-plugin解析header.txt，检查是否包含GM_addStyle等函数调用，根据是否存在这些函数，选择不同方案加载css文件

### 3. vue打包后的css在独立的文件中 - bugfix

#### 1.1.4 - vite

1. 将headerPlugin分为两部分header-load-plugin和header-post-plugin
2. header-load-plugin负责加载header到bundle中，名称为header.txt

#### 1.1.5 - vue
1. 新增plugins/vue-css.ts文件，实现vue-css-plugin插件
2. vue-css-plugin解析header.txt，检查是否包含GM_addStyle等函数调用，根据是否存在这些函数，选择不同方案加载css文件
